<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Responsi1Mobile50"
        tools:targetApi="31">

        <!-- LamanBeranda SEKARANG menjadi titik awal aplikasi -->
        <activity
            android:name=".LamanBeranda"
            android:exported="true">  <!-- WAJIB ADA -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity yang lama (jika masih ada) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Ubah ke false atau hapus baris ini -->
        <activity
            android:name=".TimActivity"
            android:exported="false" />
        <activity
            android:name=".PelatihActivity"
            android:exported="false" />
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

    </application>

</manifest>
    